{% extends 'base.html.twig' %}

{% block title %} {{ item.titre }} {% endblock %}

    {% block content %}


        <div class="row lead mt-5 mb-5 justify-content-center">
            <div class="col-md-3">
                <img src="/images/{{item.image}}" class="img-fluid rounded-start" alt="...">
            </div>
            <div class="col-md-8 my-auto">
                <h1 class="mt-7">{{ item.titre }}</h1>
                <h2 class="">{{item.auteur}}</h2>
                <h3 class=""><small class="text-muted">{{item.genre}}</small></h3>
                <p>{{ item.description }}</p>
                <a href="" class="btn btn-outline-warning">Reserver</a>

            </div>

        </div>



    {% endblock %}

